---
import { profile } from '../data/profile';

const path = Astro.url.pathname;
const isWork = path.startsWith('/work');
---

<header class="site-head">
  <div class="shell site-head__inner">
    <a class="wordmark" href="/">
      <span class="wordmark__name">{profile.name}</span>
      <span class="wordmark__role">{profile.role}</span>
    </a>

    <nav aria-label="Primary">
      <a href="/work/" aria-current={isWork ? 'page' : undefined}>Work</a>
      <a href="/#approach">Approach</a>
      <a href="/#contact">Contact</a>
    </nav>
  </div>
</header>

<style>
  .site-head {
    position: sticky;
    top: 0;
    z-index: 20;
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
  }

  .site-head__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.25rem;
    min-height: 4rem;
  }

  .wordmark {
    display: flex;
    flex-direction: column;
    gap: 0.05rem;
    text-decoration: none;
    color: var(--text-900);
    /* The orange keyline is the CV's accent, reduced to one mark. */
    border-left: 3px solid var(--orange-500);
    padding-left: 0.7rem;
  }

  .wordmark__name {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 0.9375rem;
    letter-spacing: -0.01em;
  }

  .wordmark__role {
    font-size: 0.6875rem;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    color: var(--text-500);
  }

  nav {
    display: flex;
    gap: clamp(0.9rem, 3vw, 2rem);
  }

  nav a {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-600);
    text-decoration: none;
    padding-block: 0.3rem;
    border-bottom: 2px solid transparent;
  }

  nav a:hover {
    color: var(--navy-900);
  }

  nav a[aria-current='page'] {
    color: var(--navy-900);
    border-bottom-color: var(--orange-500);
  }

  @media (max-width: 30rem) {
    .wordmark__role {
      display: none;
    }
  }
</style>
